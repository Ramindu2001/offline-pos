/**
 * Service interfaces - the contract each module offers the UI.
 *
 * Rule: interfaces only. Declare the domain exceptions a caller must handle so
 * the controller can show a real message rather than swallowing the failure.
 */
package lk.com.synsoft.offlinepos.service;
